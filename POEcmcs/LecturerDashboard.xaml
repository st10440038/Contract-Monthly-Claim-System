<UserControl  x:Class="POEcmcs.Page1"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:POEcmcs"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800">

    <Grid Margin="24" Background="LightGray">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Welcome, Lecturer" FontSize="26" FontWeight="Bold" Margin="6"/>
        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="6">
            <Button Content="Submit New Claim" Width="160" Height="36" Margin="6"
                    Command="{Binding ShowSubmitClaimCmd}"/>
            <Button Content="Upload Document" Width="160" Height="36" Margin="6"
                    Click="Upload_Click"/>
        </StackPanel>

        <GroupBox Header="My Claims" Grid.Row="2" Margin="6">
            <DataGrid ItemsSource="{Binding Claims}" AutoGenerateColumns="False" IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Month" Binding="{Binding Month}" Width="80"/>
                    <DataGridTextColumn Header="Year" Binding="{Binding Year}" Width="80"/>
                    <DataGridTextColumn Header="Hours" Binding="{Binding TotalHours}" Width="80"/>
                    <DataGridTextColumn Header="Amount" Binding="{Binding CalculatedAmount}" Width="100"/>
                    <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="140"/>
                    <DataGridTemplateColumn Header="Actions" Width="140">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="View" Click="ViewClaim_Click" Tag="{Binding}"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>
    </Grid>
</UserControl>
