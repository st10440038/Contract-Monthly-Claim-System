<Page x:Class="POEcmcs.Page1"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:POEcmcs"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Page1">

    <Grid Margin="20" Background="LightGray">
        <!-- Page Title -->
        <TextBlock Text="Claim Status Tracker" 
                   FontSize="26" FontWeight="Bold" 
                   Foreground="#1E3A8A" HorizontalAlignment="Center"/>

        <!-- DataGrid showing claims -->
        <DataGrid Name="ClaimsDataGrid" 
                  Margin="0,70,0,70"
                  AutoGenerateColumns="False" 
                  CanUserAddRows="False" 
                  CanUserDeleteRows="False"
                  HeadersVisibility="Column" 
                  IsReadOnly="True"
                  Background="White"
                  BorderBrush="#2563EB"
                  BorderThickness="1"
                  AlternatingRowBackground="#F0F4FF">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Claim ID" Binding="{Binding ClaimID}" Width="*"/>
                <DataGridTextColumn Header="Date Submitted" Binding="{Binding Date}" Width="*"/>
                <DataGridTextColumn Header="Amount" Binding="{Binding Amount}" Width="*"/>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Back Button -->
        <Button Content="← Back" 
                Width="100" Height="35"
                HorizontalAlignment="Left" 
                VerticalAlignment="Bottom" 
                Background="#1E40AF" 
                Foreground="White"
                Click="BackButton_Click"/>
    </Grid>
</Page>
